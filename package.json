{
  "name": "vanilla-js-todo-app",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight todo application built with HTML, CSS, and vanilla JavaScript.",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "check": "node scripts/check-static-files.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"index.html\" \"src/**/*.css\" \"src/**/*.js\" \"scripts/**/*.js\" \".github/**/*.yml\" \"eslint.config.mjs\" \"package.json\"",
    "format:check": "prettier --check \"index.html\" \"src/**/*.css\" \"src/**/*.js\" \"scripts/**/*.js\" \".github/**/*.yml\" \"eslint.config.mjs\" \"package.json\"",
    "build": "node scripts/build-pages-artifact.js",
    "verify": "npm run check && npm run lint && npm run format:check && npm run build",
    "start": "python3 -m http.server 4173"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "prettier": "^3.8.3"
  }
}
